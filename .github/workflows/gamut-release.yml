on:
  push:
    paths:
      - 'ts_setup/gamut/**'

    branches:
      - prod_1.x
  workflow_call: # Enables manual triggering
  workflow_dispatch: #manual triggering
permissions:
  contents: write
  id-token: write
    
name: Release Gamut prod_1.x
jobs:
    build:
      runs-on: ubuntu-latest  # windows-latest || macos-latest
      defaults:
        run:
          working-directory: ts_setup/gamut
      env:
        NODE_ENV: test
        CI: true
        
      name: Release Gamut into NPMJS 
      steps:
        - uses: actions/checkout@v4

        - uses: pnpm/action-setup@v2
          name: install pnpm
          with:
            version: 10

        - uses: actions/setup-node@v4
          with:
            node-version: 22
            cache: 'pnpm'
            cache-dependency-path: ts_setup/gamut/pnpm-lock.yaml
            registry-url: 'https://registry.npmjs.org/'

        - name: Install dependencies 📦
          run: pnpm install -r --frozen-lockfile
#        - name: Test
#          run: pnpm test -r

        - name: Run 'gamut-release.sh'  
          run: ./build-scripts/gamut-release.sh prod_1.x
          env:
            CI: false # 
            BOT_EMAIL: jocelyn.mutso@resys.io 
            BOT_NAME: jocelyn.mutso
            NODE_AUTH_TOKEN: ${{ secrets.NPMJS_TOKEN }}

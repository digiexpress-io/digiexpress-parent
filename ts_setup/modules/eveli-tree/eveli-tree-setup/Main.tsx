import React from 'react';
import { Box } from '@mui/material';

const root = { height: '100%', padding: 1, backgroundColor: "primary.contrastText" };

const Main: React.FC<{}> = () => {
  return (<Box sx={root}>MAIN</Box>)
}
export { Main }



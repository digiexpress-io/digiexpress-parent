export const helpArticleTemplate = `
# Article Template

* An Article Template is a markdown file that defines a reusable content structure for Article Pages.
* When creating an Article Page, you can select a template to pre-fill the page content with the template's markdown. You then fill in the blanks specific to that page.
* Templates are useful when many pages share the same structure — headings, sections, placeholder text — but differ in their actual content.
* Changing a template doesn't change any existing pages that were created from it. Applying a template copies its content at creation time; subsequent changes to the template do not affect existing pages.

---

## When to use

---

## Associated Assets

An Article Template has no associated assets. It is a standalone markdown document referenced at Article Page creation time.

---

## Config Options

Article Templates have no config options.
`;
